apiVersion: apps/v1
kind: Deployment
metadata:
  name: orders
  namespace: dev
  labels:
    app: orders
spec:
  replicas: 1
  selector:
    matchLabels:
      app: orders
  template:
    metadata:
      labels:
        app: orders
    spec:
      containers:
      - name: orders
        image: 303670280087.dkr.ecr.us-east-1.amazonaws.com/orders:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: dev
spec:
  selector:
    app: orders
  ports:
  - port: 80
    targetPort: 8080
  type: ClusterIP
